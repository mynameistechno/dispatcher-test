# .github/workflows/mirror.yaml
name: 'mirror'
on:
  push:
    branches:
        - __mirror
  schedule:
    - cron: '* 3 * * *'
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    name: mirror
    steps:
      - name: mirror
        id: mirror
        uses: craigez/github-mirror-action@ede8a220
        with:
          origin: 'https://github.com/craigez/foo'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TARGET_REF_SPACE: 'refs/heads/mirror/*'